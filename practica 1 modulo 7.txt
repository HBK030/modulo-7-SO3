-------------host-----------
sudo dnf install nfs-utils -y
systemctl enable nfs-server --now

------------cliente----------
sudo dnf install nfs-utils -y
sudo systemctl enable nfs-server --now
sudo dnf install nfs-utils nfs4-acl-tools -y

-----------host--------------
sudo cat /proc/fs/nfsd/versions
sudo mkdir -p /var/nfs/so3
cd /var/nfs/so3/
touch adrian{1..100}.txt
ls
cd /etc/exports.d/
nano /etc/exports

/var/nfs/so3 IP_Host(rw,sync,no_subtree_check)

sudo exportfs -arv

sudo firewall-cmd --permanent --add-service=nfs
sudo firewall-cmd --permanent --add-service=rpc-bind
sudo firewall-cmd --permanent --add-service=mountd
sudo firewall-cmd --reload

--------cliente----------
showmount -e IP_Cliente
mkdir -p /nfs/so3
sudo mount IP_Cliente:/var/nfs/so3 /nfs/so3
df -h
cd /nfs/so3/
ls
nano /etc/fstab
IP_Cliente:/var/nfs/so3 /nfs/so3 nfs auto,nofail,noatime,nolock,intr,tcp,actimeo=1800 0 0
reboot


-------------host---------------
chmod 766 /var/nfs/so3/
chown nobody:nobody /var/nfs/so3/

------------cliente------------------
ssh 2024-1913@IP_Cliente
sudo su
cd /var/nfs/so3/
ls
nano edan20241913.txt

edan wins lol

ls
cat edan20241913.txt

--------host--------
cd /var/nfs/so3/
ls
cat edan20241913.txt
